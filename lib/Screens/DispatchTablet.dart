import "package:flutter/material.dart";

class DispatchTabletPage extends StatelessWidget {
  const DispatchTabletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Dispatch tablet"),
    );
  }
}
